<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Drivers</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

<h1>Drivers List</h1>

<a href="/">← Back to Register</a>

<table border="1" cellpadding="10">
  <tr>
    <th>#</th>
    <th>Code</th>
    <th>Name</th>
    <th>DOB</th>
    <th>Nationality</th>
    <th>Team</th>
    <th>URL</th>
    <th>Actions</th>
  </tr>

  <% drivers.forEach(d => { %>
    <tr>
      <td><%= d.num %></td>
      <td><%= d.code %></td>
      <td><%= d.forename %> <%= d.surname %></td>
      <td><%= d.dob.toISOString().split("T")[0] %></td>
      <td><%= d.nationality %></td>
      <td><%= d.team ? d.team.name : "N/A" %></td>
      <td><a href="<%= d.url %>" target="_blank">link</a></td>
      <td>
        <a href="/driver/edit/<%= d._id %>">Edit</a>
        |
        <a href="/driver/delete/<%= d._id %>" onclick="return confirm('Delete this driver?')">Delete</a>
      </td>
    </tr>
  <% }); %>

</table>

</body>
</html>
